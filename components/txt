<div className={`w-full ${layout.sectionRow} gap-8`}>

{/* صورة هنكا */}
<div className={`${layout.sectionImg}`}>
  <Image
    src={henkaImg}
    alt="Henka Image"
    width={550}
    height={550}
    className="rounded-[30px]"
  />
</div>

{/* المحتوى النصي */}
<div className={`${layout.sectionInfo}`}>
  <div className="border border-[#222] bg-[#fff] rounded-2xl px-6 py-8">
    <h2 className="text-2xl md:text-3xl text-black font-semibold mb-6 text-center">
      لماذا هنكا؟
    </h2>
    <ul className="space-y-4 list-disc pr-6 text-lg leading-relaxed">
      {why_henka.points.map((point, index) => (
        <li key={index} className="text-textColor">
          {point}
        </li>
      ))}
    </ul>
  </div>
</div>

</div>